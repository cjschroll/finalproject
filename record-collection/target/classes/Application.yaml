spring:
  datasource:
    username: record_collection
    password: record_collection
    url: jdbc:mysql://localhost:3306/record_collection
   
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
   
  sql:
    init:
      mode: never
         